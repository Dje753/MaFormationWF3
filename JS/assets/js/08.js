/*--------------------------------------------
        LES CONDITIONS
---------------------------------------------*/

/*var MajoriteLegaleFR = 18;

if(16 > MajoriteLegaleFR){
        alert("Bienvenu !");
} else {
    alert("Google...");
} */

// ma version 
/* var MajoriteLegaleFR = 18;


function AgeLeg(){
    // -- je lui demande son age 
    let age = prompt("Bonjour, quel âge tu ?","<saisi ton âge>");
    // -- si il a plus de 18 ans alors bienvenu
    if(age > MajoriteLegaleFR){
        alert("Bienvenu !");
    // -- si il a monis de 18 alors explication + redirection google
    }else(18 < MajoriteLegaleFR);{ 
        alert("Désoler mais tu n'est pas majeur, tu ne peux donc pas acceder à ce site de Zoophilie")
        document.location.href="http://www.google.com"
    }
}
AgeLeg() */

/*
// -- Correction de l'erxercice

// -- 1 Déclarer La majorité légale 
var MajoriteLegaleFR = 18;

// 2-- Créer une fonction pour demander son age
function verifierAge(){
    // -- Demande l'age de mon visiteur puis je le retourne
    return parseInt(prompt("Bonjour, quel age avez-vous ?","<Saisissez votre Age"));

}

// 3 -- Une condition pour vérifier si l'age de l'utilisateur est supérieur à la majoritelegalefr
if(verifierAge() >= MajoriteLegaleFR) {
    // -- j'affiche un message de bienvenu
    alert("Bienvenu sur mon site internet pour les majeurs...");
} else {
    // -- J'affiche une alert
    alert("ATTENTION ! ! ! Vous devez être majeur pour acceder à ce site !");

    // -- Je redirige vers google.
    document.location.href = "http://lmgtfy.com/?q=majorite+l%C3%A9gale+fran%C3%A7aise"
}

*/


/* -----------------
    LES OPERATEURS DE COMPARAISON
    ----------------------*/

    // -- L'opérateur de comparaison "==" signifie : Egale à ...
    //  Il permet de vérifier que deux variables sont identiques.

    // -- L'opérateur de comparaison "===" signifie : Strictement Egale à ...
    //  Il va comparer la valeur et aussi le type de donné.

    // -- L'opérateur de comparaison "!=" signifie: Différent de

    // -- L'opérateur de comparaison "!==" signifie: Strictement Différent de


    // -- EXERCICE


    // -- Base de données
    var email, mdp;

    email = "wf3@h1-media.fr"
    mdp   = "wf3"

    
/*
function authentification(){
    // -- je demande le mail et mdp
    return prompt("Bonjour ! veuillez saisir votre mail","<mail>", "et votre Mot de Passe.","<mdp>");

}
     // -- si pas bon echec de saisi avec un mot
     if(email != email, mdp != mdp){
     alert("Identifiant(s) incorrect")
} else (email == email, mdp == md)
    //-- si identique base de données alors ok et messge bienvenu */


/*------------------------------------------------
    LES OPERATEURS DE LOGIQUES
--------------------------------------------------*/

// L'opérateur ET : && ou AND

// L'operateur OU : || ou OR

// L'opérateur "!" : qui signifie le CONTRAIRE de, ou encore NOT


























